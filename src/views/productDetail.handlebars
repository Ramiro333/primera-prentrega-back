<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
</head>
<body>
    <h1>{{product.title}}</h1>
    <p>Descripción: {{product.description}}</p>
    <p>Codigo: {{product.code}}</p>
    <p>Precio: ${{product.price}}</p>
    <p>status: {{product.status}}</p>
    <p>Categoría: {{product.category}}</p>
    <p>Stock: {{product.stock}}</p>
    <p>hora de creacion: {{product.createdAt}}</p>
    <p>hora de modificacion: {{product.updatedAt}}</p>
    <button class="add-to-cart" data-product-id="{{product._id}}">Agregar al carrito</button>
    <a href="/">Volver al inicio</a>
</body>
</html>
<script src="/api/public/js/productDetail.js"></script>